name: PR Build

on:
  pull_request:
    branches: [ main ]

jobs:
  build:
    uses: ./.github/workflows/_shared-build.yml
    with:
      run-lint: true
      run-honey: true
      run-tests: true
      run-publish: false
    secrets: inherit
